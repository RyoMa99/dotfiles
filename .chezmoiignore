は# Claude Code: secrets (managed manually via run_once script)
.claude/settings.local.json

# Claude Code: runtime data (machine-local, auto-generated)
.claude/cache
.claude/chrome
.claude/debug
.claude/file-history
.claude/ide
.claude/memories
.claude/paste-cache
.claude/plans
.claude/plugins
.claude/projects
.claude/session-env
.claude/shell-snapshots
.claude/statsig
.claude/tasks
.claude/telemetry
.claude/todos

# Claude Code: external skills (managed by .chezmoiexternal.toml)
.claude/skills/notebooklm

# Claude Code: agent skills (managed by npx skills add via run_once script)
{{ if stat (joinPath .chezmoi.homeDir ".agents/skills") -}}
{{ range (output "ls" (joinPath .chezmoi.homeDir ".agents/skills") | trim | splitList "\n") -}}
.claude/skills/{{ . }}
{{ end -}}
{{ end -}}

# tmuxinator: machine-local config
.config/tmuxinator/work.yml
