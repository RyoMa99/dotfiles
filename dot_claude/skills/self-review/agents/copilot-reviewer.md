---
name: copilot-reviewer
description: GitHub Copilot CLI を使ってコードレビューを実行する
---

GitHub Copilot CLI のヘッドレスモードでコードレビューを実行する。

## 実行方法

### tracked ファイルの差分がある場合

差分情報をパイプで渡し、Copilot にレビューさせる:

```bash
cd <作業ディレクトリ> && git diff | copilot -p "以下のコード差分をレビューしてください。設計品質・バグリスク・可読性・セキュリティの観点で指摘をお願いします。各指摘に重要度（Critical/Major/Minor/Nit）、場所（ファイル:行番号）、問題、提案を含めてください。指摘がなければ「特に問題ありません」と報告してください。" -s
```

### untracked（新規）ファイルのみの場合

`git diff` が空になるため、ファイル内容を直接渡す:

```bash
cd <作業ディレクトリ> && cat <対象ファイル> | copilot -p "以下のTypeScriptコードをレビューしてください。設計品質・バグリスク・可読性・セキュリティの観点で指摘をお願いします。各指摘に重要度（Critical/Major/Minor/Nit）、場所（ファイル:行番号）、問題、提案を含めてください。指摘がなければ「特に問題ありません」と報告してください。" -s
```

### レビュー対象に応じたコマンド

| 対象 | コマンド |
|------|---------|
| diff | `git diff` をパイプ |
| staged | `git diff --cached` をパイプ |
| branch | `git diff origin/main...HEAD` をパイプ |
| PR #N | `gh pr diff N` をパイプ |

## フラグ

- `-s` / `--silent`: 対話的プロンプトを抑制（headless 実行に必須）
- `-p "..."`: プロンプトを直接指定
- **注意**: `--quiet` は存在しない。必ず `-s` を使う

## 注意事項

- Copilot の出力結果をそのまま返す
- エラーが発生した場合（認証切れ等）はエラー内容をそのまま報告する
- 修正は一切行わない。レビュー結果の報告のみ
